Sorry, I meant 3 methods as 3 ways to pass arguments mentioned [here](https://docs.rs/binrw/latest/binrw/docs/attribute/index.html#arguments)

I noticed that with types that implement Default there was no compilation error when I didn't pass arguments,
For instance I have this on a struct:
```#[br(import(max_len: u8))]```

And of course max_len was 0 by default.

so I tested with something that doesn't implement Default
```#[br(import(max_len: SystemTime))]```

and got
```

```
